select * from emp

select * from dept

1.SELECT * FROM EMP WHERE SAL= (SELECT MAX(SAL) FROM EMP WHERE DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE DNAME='ACCOUNTING'))

2.SELECT ENAME FROM EMP WHERE SAL=(SELECT MIN(SAL) FROM EMP WHERE JOB='MANAGER')

3.SELECT DNAME FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMP GROUP BY DEPTNO HAVING COUNT(*)>2)

4.SELECT ENAME FROM EMP WHERE JOB='MANAGER' AND MGR!=(SELECT EMPNO FROM EMP WHERE JOB='PRESIDENT')

5.SELECT * FROM EMP WHERE (DEPTNO,SAL) IN(SELECT DEPTNO,SAL FROM EMP WHERE COMM IS NOT NULL)

6.SELECT * FROM EMP WHERE DEPTNO IN(10,20) AND JOBIN('CLERK','ANALYST') AND SALIN(ROUND(SAL,2),ROUND(SAL,2))

7.SELECT ENAME FROM EMP WHERE  MGR=(SELECT EMPNO FROM EMP WHERE ENAME='BLAKE' AND HIREDATE=(SELECT MIN(HIREDATE) FROM EMP))